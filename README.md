# SDF
SDF is a rule-based decision engine that produces deterministic outputs from non-deterministic LLM models. It applies verification engineering discipline — hard-gate classification constraints, weighted scoring logic, and iterative failure mode documentation — to make AI-assisted reasoning reliable, traceable, and bounded. Currently at v2.3
